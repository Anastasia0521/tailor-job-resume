---
name: tailor-job-resume
description: 根据岗位信息、综合简历、论文及公开招聘市场，识别科研人员的数据、方法、软件和可迁移能力，研究完整职业边界与薪资，并制作针对性的可编辑 HTML 简历、求职策略报告及对话内个人总结。仅在用户明确提出完整句子“请根据这个岗位帮我制作针对性简历”时使用；不要因普通简历咨询、简历润色、岗位分析、HTML 编辑或相似但不完整的请求触发。
---

# 岗位针对性简历

严格按以下流程执行。只优化表达与结构，不改变事实性质。

## 1. 检查输入

要求用户提供：

- 招聘单位、岗位名称、岗位职责和任职要求，可来自截图、网页、PDF、Word 或粘贴文本。
- 至少一份综合简历，可为 DOCX、PDF、HTML、Markdown 或纯文本。
- 输出目录。

照片与参考模板为可选项。缺少必要输入且无法从本轮上下文或文件中定位时，一次性列出缺失项。不要反复索取已提供材料。

读取 [references/input-checklist.md](references/input-checklist.md) 处理输入与默认值。

## 2. 提取并匹配

完整提取岗位要求和简历事实。将要求拆为硬性门槛、核心职责、研究/业务能力、方法工具、协作表达和优先条件。

建立“岗位要求—履历证据—可信程度—表达策略”矩阵。优先采用直接证据，其次采用可迁移证据；无证据要求必须明确标记，不得补写。

读取 [references/matching-framework.md](references/matching-framework.md) 执行排序和改写。

不要只按研究主题匹配。简历中存在论文或研究项目时，读取 [references/research-capability-extraction.md](references/research-capability-extraction.md)，自动获取论文资料，提取数据、方法、软件、分析流程和成果，并翻译成企业任务能力。

在最终写作前读取 [references/evidence-confirmation.md](references/evidence-confirmation.md)，集中展示 A、B、C 级证据，让用户只确认事实。首次分析不询问行业、企业性质、城市或薪资偏好，不让信息不足的偏好提前缩小职业边界。

## 3. 守住事实与隐私边界

读取 [references/fact-boundaries.md](references/fact-boundaries.md)。强制遵守：

- 不把参与改为主持，不把协助改为独立负责。
- 不编造数据、奖项、论文状态、任职时间或成果影响。
- 区分已发表、录用、在投、待投和工作论文。
- 区分个人贡献与团队成果。
- 不向公开仓库、第三方网站或无关输出复制个人材料。
- 不将客户简历、照片、电话、邮箱或本次输出写入本 Skill 目录。

## 4. 生成内容

按招聘方阅读顺序组织：个人概述、岗位匹配能力、教育背景、重点经历/项目、代表成果、方法技能、荣誉与协作。保留所有有利且不重复的证据，删除或合并弱相关内容。

每段优先采用“角色—任务—方法—成果—价值”结构。突出与岗位直接相关的名词和动词，避免空泛自评。

正文使用正式、精简的陈述句。个人概述和经历描述不得使用中文或英文引号，不得使用乘号 `×`、`✕` 或加号 `+`。将符号连接改为完整、自然的书面表达，例如用“并”“以及”“同时”说明并列关系。此限制不适用于必须忠实保留的论文正式题名、机构名称和技术专名。

在对话中另行提供一段 200-350 字的针对性个人总结。

### 科研论文与 DOI

简历中存在科研论文时，读取 [references/paper-doi-rules.md](references/paper-doi-rules.md) 并逐篇执行：

- 使用论文完整题目检索 DOI，优先核对出版社论文页和 Crossref 元数据。
- 中文期刊论文必须先按期刊名定位官网，再在官网用完整中文题名查找文章详情页并提取 DOI；普通搜索或 Crossref 无结果不能作为停止条件。详情见 [references/paper-doi-rules.md](references/paper-doi-rules.md)。
- 至少核对标准化后的题目，并尽可能核对作者、期刊和年份；只有高置信度匹配时才写入 DOI。
- 每篇论文必须单独核查，不得仅因原简历未写 DOI 而跳过检索。将检索所用的完整题目与最终采用的 DOI 记录到工作数据中。
- 将已验证 DOI 规范化为 `10.xxxx/...`。HTML 中紧跟论文标题放置超链接，仅显示 `DOI` 三个字母，链接目标为 `https://doi.org/10.xxxx/...`，不得展示完整网址或 DOI 字符串。
- 未找到或存在冲突时不得猜测；简历中省略 DOI，并在岗位匹配分析中列入待确认项。
- 将所有识别为论文的成果写为 `kind: "paper"`，并分别填写 `title`、`doi`、`journal`、`level`、`author_rank`、`year`。不得把这些字段合并到 `heading` 或 `meta`。
- DOI 已确认时同时填写 `doi_source_url`，保存期刊官网文章页、官网 PDF、出版社页面或 Crossref 记录地址；不在简历中展示该地址。
- 每篇论文严格使用单行顺序：`标题或省略标题｜DOI｜期刊名称（等级）｜第几作者｜年份`。不另起详情行，不显示完整作者名单。
- 期刊、等级、第几作者和年份必须完整显示；无法核验时填“待确认”并列入岗位匹配分析，不得省略或推断。只有标题可以为适配宽度自动截断。
- 不要手工按固定词数截断标题，交给 HTML 生成器按实际可用宽度处理。验证标题与 DOI 链接保持单行；标题过长时必须在词或字符边界以 `…` 结尾。

## 5. 研究完整职业边界

读取 [references/career-boundary-analysis.md](references/career-boundary-analysis.md)。根据已确认能力主动识别所有有实质证据支持的直接方向、迁移方向和拓展方向；识别出几个就研究几个，不要求用户预先选择。

读取 [references/job-market-and-salary-research.md](references/job-market-and-salary-research.md)。逐个方向搜索近期公开岗位，说明工作内容、行业与公司类型、常见岗位名、市场需求、薪资、当前优势、缺口、简历策略和面试风险。不得要求用户自己爬取招聘网站或提供登录凭据。

生成详细的 `求职策略报告.md`。首页先用通俗语言回答最值得尝试什么、还能尝试什么、暂不建议什么、大致薪资和下一步行动。正文保留全部已识别方向，不以评分代替解释，不使用研究报告腔。

## 6. 生成唯一 HTML 交付物

只生成：

`姓名_单位_岗位_可编辑版.html`

不得额外生成 `姓名_单位_岗位_针对性简历.html`。

同时生成 `岗位匹配分析.md` 和 `求职策略报告.md`。若照片未嵌入 HTML，则将照片副本放在同一输出目录。

先将结构化内容写入 JSON，再运行：

```powershell
python scripts/build_editable_resume.py --input resume.json --output "姓名_单位_岗位_可编辑版.html" --photo photo.jpg
python scripts/validate_resume_html.py --input resume.json --html "姓名_单位_岗位_可编辑版.html"
python scripts/validate_strategy_report.py --report "求职策略报告.md"
```

JSON 字段见 [references/input-checklist.md](references/input-checklist.md)。生成的 HTML 必须支持：直接编辑文字、自动暂存、预览切换、撤销、下载 HTML、打印/PDF，以及照片宽度、高度、左右位置、上下位置和裁切调整。

必须使用生成脚本处理论文条目，不得绕过脚本把论文手写进普通标题元素。验证脚本未通过时禁止交付。每份 HTML 使用独立的 v5 暂存键，不得读取旧版缓存。

## 7. 验证

用浏览器渲染 HTML，并检查：

- 照片正常加载；无照片时无破损图标。
- 各页无重叠、裁切和异常空白。
- 每篇论文均已执行 DOI 检索；论文单行包含题目、可点击的 `DOI`、期刊名称与等级、第几作者和年份。链接目标与可信来源一致，页面不显示 DOI 字符串或完整网址。
- 论文标题行无换行；截断标题以 `…` 结尾，`DOI` 链接完整可见并能跳转。
- 论文首行两端对齐：标题左边缘贴齐版心左侧，年份右边缘贴齐版心右侧；仅标题可删减，其他四项不得截断。
- 个人概述与经历描述不含引号、乘号或加号。
- 编辑控件可用，打印时全部隐藏。
- 调整照片后文字布局仍可接受。
- 下载后的 HTML 能再次打开和编辑。
- 策略报告覆盖全部已识别方向，并逐个说明实际工作、匹配证据、近期岗位、薪资参考、能力缺口、简历和面试建议。
- 薪资说明包含城市、时间、样本数量、月薪或年薪口径和来源；证据不足时明确说明只能初步参考。
- 报告通俗易懂；除必要的方法名称外不使用学术化术语，专业词后紧跟白话解释。

运行隐私扫描：

```powershell
python scripts/privacy_scan.py <skill-or-output-directory>
```

只有扫描通过且视觉检查无缺陷时才交付。

